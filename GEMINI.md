# Antigravity Agent Rules

## General Workflow Guidelines
- **Always push changes to GitHub**: Whenever you make modifications to the codebase and complete a task, you MUST commit and push those changes to the remote repository.
- **Always deploy a local test**: After making changes, you MUST always deploy a local test using the local test `docker-compose.local.yml` file to verify that the containerized environment runs properly.
